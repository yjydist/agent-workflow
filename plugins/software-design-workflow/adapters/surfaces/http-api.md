# HTTP API adapter

## Layer

Surface

## Applies when

Use when clients interact through HTTP resources, RPC endpoints, webhooks, or REST-like contracts.

## Design focus

Define endpoints, request and response schemas, status codes, authentication, rate limits, idempotency, pagination, and versioning.

## Expected artifacts

API contract, auth model, error envelope, status-code policy, examples.

## Legacy migration

Legacy `backend-api` API content migrates here when it describes endpoint-level behavior.
