# TUI adapter

## Layer

Surface

## Applies when

Use when terminal interaction needs persistent screens, keyboard navigation, panes, progress, or interactive selection.

## Design focus

Define screen states, keybindings, focus behavior, terminal capabilities, resize handling, and fallback CLI behavior.

## Expected artifacts

TUI screen map, keybinding table, state transitions, terminal compatibility notes.
