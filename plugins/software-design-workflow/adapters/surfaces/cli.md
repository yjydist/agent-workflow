# CLI adapter

## Layer

Surface

## Applies when

Use when people or scripts interact through terminal commands, flags, stdin, stdout, stderr, config files, or exit codes.

## Design focus

Define command hierarchy, flags, config precedence, input and output formats, errors, exit codes, and scripting guarantees.

## Expected artifacts

Command reference, flag matrix, config model, IO contracts, exit-code table.
