# IPC adapter

## Layer

Surface

## Applies when

Use when components communicate across processes, workers, plugins, extensions, or local services.

## Design focus

Define channels, message schemas, lifecycle, permissions, timeouts, versioning, and failure handling.

## Expected artifacts

Channel map, message contracts, lifecycle and compatibility notes.
