# Background Jobs adapter

## Layer

Surface

## Applies when

Use when work happens asynchronously through schedulers, queues, cron, workers, or long-running tasks.

## Design focus

Define job triggers, payloads, retry policy, idempotency, progress, cancellation, observability, and failure recovery.

## Expected artifacts

Job catalog, trigger map, payload schemas, retry and recovery policy.
