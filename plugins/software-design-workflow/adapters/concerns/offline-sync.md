# Offline Sync concern adapter

## Layer

Concern

## Applies when

Use when local changes, caches, queues, or replicas must reconcile with remote state.

## Design focus

Define sync ownership, conflict rules, merge behavior, retry windows, tombstones, recovery UX, and consistency guarantees.

## Expected artifacts

Sync model, conflict policy, queue semantics, recovery scenarios.
