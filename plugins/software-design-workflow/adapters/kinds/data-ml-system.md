# Data ML System adapter

## Layer

Kind

## Applies when

Use when the software ingests, transforms, validates, trains, scores, or serves data and machine learning workflows.

## Design focus

Define data sources, schemas, lineage, transformations, scheduling, model lifecycle, validation, and drift or quality checks.

## Expected artifacts

Pipeline map, schema contracts, scheduling, validation, observability, output contracts.

## Legacy migration

Legacy `data-pipeline` maps here and may combine `background-jobs`, `event-stream`, `file-format`, `server`, and `observability` adapters.
