# Language Compiler adapter

## Layer

Kind

## Applies when

Use when the software parses, analyzes, transforms, compiles, interprets, formats, or validates a language or DSL.

## Design focus

Define grammar, semantic model, diagnostics, target output, compatibility, extensibility, and test corpus.

## Expected artifacts

Language spec, AST or IR model, diagnostic contracts, compatibility policy, conformance tests.
