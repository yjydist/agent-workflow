# Edge Runtime adapter

## Layer

Runtime

## Applies when

Use when code runs close to users in CDN workers, edge functions, constrained isolates, or regional edge nodes.

## Design focus

Define runtime limitations, data locality, cold starts, caching, regional consistency, and fallback behavior.

## Expected artifacts

Edge capability matrix, caching strategy, data locality notes, fallback plan.
