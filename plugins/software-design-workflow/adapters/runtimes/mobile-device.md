# Mobile Device runtime adapter

## Layer

Runtime

## Applies when

Use when code runs on phones, tablets, or app runtimes with lifecycle, permissions, sensors, and app-store constraints.

## Design focus

Define platform versions, permissions, background limits, storage, network variability, push notifications, and release channels.

## Expected artifacts

Platform matrix, permission model, storage and sync behavior, release constraints.
