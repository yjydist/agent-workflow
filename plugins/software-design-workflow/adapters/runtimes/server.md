# Server runtime adapter

## Layer

Runtime

## Applies when

Use when code runs on hosted servers, containers, VMs, serverless functions, or managed backends.

## Design focus

Define deployment topology, configuration, secrets, scaling, persistence connections, health checks, logging, and rollout strategy.

## Expected artifacts

Deployment-runtime, config model, health checks, scaling assumptions, operational notes.

## Legacy migration

Legacy `backend-api` deployment content migrates here.
