# Execution brief

## Objective

A local CLI scans a repository, identifies generated or stale artifacts, and writes a reviewable cleanup plan without deleting files.

## Current target

Implement only the documented target slice and preserve the constraints, risks, and open questions.
