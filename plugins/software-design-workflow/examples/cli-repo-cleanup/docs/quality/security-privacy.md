# Security and privacy

The tool reads local files only under requested paths and never transmits repository content.

The default posture is least privilege and explicit user control over sensitive inputs.
