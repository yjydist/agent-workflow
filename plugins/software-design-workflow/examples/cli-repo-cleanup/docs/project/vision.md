# Vision

Build a repository cleanup assistant that helps maintainers identify stale generated files without mutating the repository.

## Target user

A developer or maintainer who wants a safe first pass before approving cleanup actions manually.

## Product outcome

The tool scans a repository, explains why files are candidates, and emits a plan the user can review outside the tool.
