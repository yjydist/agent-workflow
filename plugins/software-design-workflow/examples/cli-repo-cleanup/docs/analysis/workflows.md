# Workflows

## Primary workflow

User runs scan, tool reads allowed paths, rules classify candidates, report is written, maintainer reviews actions manually.

## Exception workflow

If required evidence is missing, stop the flow and report what the user must decide before continuing.
