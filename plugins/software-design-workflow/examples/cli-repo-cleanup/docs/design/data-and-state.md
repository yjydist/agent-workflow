# Data and state

State is in memory during scan. Reports are written to stdout or an output file selected by the user.

The current target avoids hidden persistent mutation unless a user explicitly selects an output path.
