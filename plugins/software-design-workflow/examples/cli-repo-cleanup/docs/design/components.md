# Components

CLI adapter, filesystem scanner, rule registry, evidence collector, report renderer.

Each component owns one responsibility and exposes a small contract that can be tested independently.
