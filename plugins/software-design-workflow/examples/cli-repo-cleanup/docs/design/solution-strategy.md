# Solution strategy

Prefer conservative detection, no mutation by default, and explicit evidence per finding.

The design intentionally keeps the first target narrow so handoff agents can complete it without changing product scope.
