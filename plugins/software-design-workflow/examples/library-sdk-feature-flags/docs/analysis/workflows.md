# Workflows

## Primary workflow

Consumer loads config, creates client, evaluates a flag with context, receives value and reason.

## Exception workflow

If required evidence is missing, stop the flow and report what the user must decide before continuing.
