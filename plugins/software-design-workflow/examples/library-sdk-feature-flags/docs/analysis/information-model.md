# Information model

Inputs are JSON config and optional context attributes. Outputs are typed values and fallback explanations.

Each output field needs enough provenance for a follow-up agent to verify or reproduce it.
