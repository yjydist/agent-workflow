# Domain model

Core entities: FlagSet, FlagDefinition, EvaluationContext, EvaluationResult, FallbackReason.

The model keeps terms small enough for implementation agents to map directly into code and tests.
