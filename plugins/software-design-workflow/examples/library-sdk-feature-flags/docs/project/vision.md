# Vision

Build a small feature flag SDK that evaluates local JSON configuration with a stable public API.

## Target user

A backend or tooling developer who needs local flag evaluation before introducing any hosted service.

## Product outcome

Consumers load a JSON flag set once, evaluate flags synchronously, and receive typed results that explain fallbacks.
