# Project Context

This example is a miniature handoff package for a library or SDK workflow. The documents should be coherent enough that an implementation agent can build the target while respecting API stability boundaries.

The main design constraint is compatibility: public method names, result shape, and JSON file expectations must stay predictable within the target.
