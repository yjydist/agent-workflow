# Open Questions

- Should attributes remain a generic key-value map in this target, or should the SDK constrain supported attribute types more tightly?
- Do compatibility fixtures need a version field in the JSON file now, or can that wait for a later target?
- Should invalid but unused flag definitions fail the whole file, or only the affected flag entries?
