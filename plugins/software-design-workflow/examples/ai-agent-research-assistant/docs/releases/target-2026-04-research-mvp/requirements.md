# Requirements

1. The workflow must accept a single research topic and optional audience hint.
2. The workflow must record source metadata for every captured note.
3. The workflow must generate a brief that includes citations and a short unresolved-questions section.
4. The workflow must refuse unsupported source access instead of silently skipping policy limits.
