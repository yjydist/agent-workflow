# Data and state

Session notes are temporary. Saved output is a markdown brief plus source list.

The current target avoids hidden persistent mutation unless a user explicitly selects an output path.
