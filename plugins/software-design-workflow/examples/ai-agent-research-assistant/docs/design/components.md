# Components

Conversation adapter, research planner, source collector, note store, brief writer, citation checker.

Each component owns one responsibility and exposes a small contract that can be tested independently.
