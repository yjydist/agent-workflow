# Project Context

This example represents a handoff-ready package for a design workflow plugin. The package should be realistic enough for an implementation agent to execute without inventing missing documents.

The main design constraint is bounded behavior: the agent must stay inside approved source policies, preserve citations, and stop when scope reaches automation or integration work.
